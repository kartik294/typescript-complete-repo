"use strict";
// to run the file in watch mode use tsc app.ts --watch or -w
// combining mulriple tyescrip filesm first initialize 1) tsc --init 2)use tsc to compile all ts files to js 3)just use tsc
console.log("Typescript compiler configuration");
